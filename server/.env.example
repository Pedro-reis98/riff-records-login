PORT=3333
DATABASE_URL=postgres://riffrecords:riffrecords@localhost:5432/riffrecords
JWT_SECRET=troque-este-segredo-com-pelo-menos-16-caracteres
CORS_ORIGIN=http://localhost:8081,http://localhost:19006,http://localhost:3000
DB_SSL=false
APP_URL=http://localhost:8081

# Recomendado para Render free: Brevo API por HTTPS.
BREVO_API_KEY=
MAIL_FROM="Riff Records <no-reply@seudominio.com>"

# Fallback SMTP com Nodemailer. Use localmente ou em hospedagens que liberam SMTP.
# Brevo SMTP: host smtp-relay.brevo.com, porta 587.
# Sem BREVO_API_KEY e sem SMTP, a API registra o e-mail no console em modo dev.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
